#!/bin/sh
# pre-commit — lint gate
# Zero errors, zero warnings. A clean codebase stays clean.

set -e

npm run lint

# No userland graph algorithms — use git-warp's graph.traverse.* instead
bash scripts/check-graph-algorithms.sh
