/**
 * @deprecated — Moved to infrastructure layer.
 *
 * The implementation now lives in:
 *   src/infrastructure/adapters/TsCompilerTestParserAdapter.ts
 *
 * This file remains as a tombstone to prevent accidental re-creation.
 * Import from the adapter or use the TestParserPort interface instead.
 *
 * Hexagonal Architecture Audit — H2 fix.
 */

export {};
