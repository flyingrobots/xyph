Resume repo recruiter prep needed several separate XYPH reads: roadmap, lineage, search, history, writer refs, and raw git-warp inspection. A native MCP read surface would have made this graph state directly queryable without shell parsing or special-case CLI glue.